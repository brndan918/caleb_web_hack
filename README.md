# 使用方法:

- 右鍵 -> 檢查 (或者是ctrl+shift+i)
- 切換到console頁面
- 手動輸入`allow pasting`
- 輸入以下代碼:
```eval(await(await fetch('https://raw.githubusercontent.com/brndan918/caleb_web_hack/refs/heads/main/all_func.txt')).text())```
- 使用help()查看完整教學
- 透過 `方法()` 的方式進行破解

免責聲明: 這是一項破解器 使用請自己承擔風險
